# @trickshot/api (deprecated)

This Fastify health stub predates the stack lock change to **`backend=supabase`**.

**Do not grow new features here.** Implement API surface as Supabase Edge Functions under [`supabase/functions`](../../supabase/README.md).

Removal tracked with backend cutover issues (#6–#10).
