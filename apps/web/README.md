# @trickshot/web

Phaser 3 PWA client (`client=phaser_pwa`). Custom 2D physics lands in Alpha — match [docs/animation-pitch.html](../../docs/animation-pitch.html) before feature work.

```bash
# from repo root
npm install
npm run dev:web
```
