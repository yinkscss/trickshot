# Deprecated for Alpha backend.
# Stack lock is backend=supabase — use `npx supabase start` instead of this compose file.
# Kept only as a temporary reference; prefer deleting once Edge cutover is complete.
services: {}
